apiVersion: v1
kind: Pod
metadata:
  name: test-bad-pod
  namespace: cks-secure-app
  annotations:
    cks.exercise: "01-secure-app-deploy"
    cks.task: "Test Kyverno policy enforcement"
    cks.expected: "This pod should be REJECTED"
spec:
  containers:
  - name: hacker
    image: busybox:latest
    command: ["sleep", "3600"]
    securityContext:
      privileged: true  # This violates the policy